/**
 * TUI 组件测试占位文件
 *
 * 注：ink-testing-library 在 Node 18 环境有兼容性问题，
 * 暂时跳过具体测试，待后续修复
 */

import { describe, it, expect } from "vitest";

describe("TUI Components", () => {
  it("placeholder test - 测试框架正常工作", () => {
    expect(true).toBe(true);
  });
});
